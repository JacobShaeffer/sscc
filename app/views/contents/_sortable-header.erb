<!--Sortable header for content table-->
<th>
    <%= sortable_link_contents(sort: column, label: title) %>
    <%= sort_indicator(filters: "content_filters") if session.dig("content_filters", 'sort') == column %>  
</th>