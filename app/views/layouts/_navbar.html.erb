<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to root_path, class:"navbar-brand" do %>
      <%= image_tag("Vertical-White-Sunglow.png", alt:"SolarSPELL vertical logo", class:"d-inline-block align-text-center me-2", width:"55", height:"74") %>
      SPELL-CC
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item me-2">
          <%= link_to 'Metadata', metadata_types_path, class: 'nav-link ' + (current_controller?('metadata_types') ? 'active text-secondary' : '') %>
        </li>
        <li class="nav-item me-2">
          <%= link_to 'Content', contents_path, class: 'nav-link ' + (current_controller?('contents') ? 'active text-secondary' : '') %>
        </li>
        <li class="nav-item me-2">
          <%= link_to 'Copyright', copyright_permissions_path, class: 'nav-link ' + (current_controller?('copyright_permissions') ? 'active text-secondary' : '') %>
        </li>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
				<% if user_signed_in? %>
					<li class="nav-item me-2">
						<%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn btn-secondary' %>
					</li>
          <li class="nav-item me-2">
            <%= link_to 'Profile', profile_path, class: 'nav-link ' + (current_controller?('devise/registrations') ? 'active text-secondary' : '') %>
          </li>
				<% else %>
					<li class="nav-item me-2">
						<%= button_to "Sign Up", home_join_path, method: :get, class: 'btn btn-secondary' %>
					</li>
					<li class="nav-item me-2">
						<%= link_to "Login", new_user_session_path, class: 'nav-link' %>
					</li>
				<% end %>
      </ul>
    </div>
  </div>
</nav>