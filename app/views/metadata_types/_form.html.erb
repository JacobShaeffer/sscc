<%= form_with(model: metadata_type) do |form| %>
  <% if metadata_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(metadata_type.errors.count, "error") %> prohibited this metadata_type from being saved:</h2>

      <ul>
        <% metadata_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
